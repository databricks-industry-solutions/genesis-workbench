resources:
  jobs:
    gwas_initial_setup_job:
      name: dbx_gwas_initial_setup

      email_notifications:
        on_failure:
          - ${var.current_user}

      tasks:
        - task_key: download_reference_genome
          max_retries: 0
          environment_key: default
          notebook_task:
            notebook_path: ../notebooks/01_download_reference_genome.py
            source: WORKSPACE

        - task_key: initialize
          disable_auto_optimization: true
          environment_key: default
          depends_on:
            - task_key: download_reference_genome
          notebook_task:
            notebook_path: ../notebooks/initialize.py
          max_retries: 0
          timeout_seconds: 600

      environments:
        - environment_key: default
          spec:
            client: '4'

      parameters:
        - name: catalog
          default: ${var.core_catalog_name}
        - name: schema
          default: ${var.core_schema_name}
        - name: sql_warehouse_id
          default: ${var.sql_warehouse_id}
        - name: parabricks_alignment_job_id
          default: ${resources.jobs.parabricks_alignment.id}
        - name: gwas_analysis_job_id
          default: ${resources.jobs.gwas_analysis.id}
        - name: user_email
          default: ${var.current_user}
        - name: databricks_app_names
          default: genesis-workbench:mcp-genesis-workbench

      tags: ${var.common_resource_tags}
