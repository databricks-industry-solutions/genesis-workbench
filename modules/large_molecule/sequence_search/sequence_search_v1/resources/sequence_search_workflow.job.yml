resources:
  jobs:
    sequence_search_workflow:
      name: sequence_search_workflow

      email_notifications:
        on_failure:
          - ${var.current_user}

      tasks:

        - task_key: download_sequences
          max_retries: 0
          job_cluster_key: cpu_cluster
          notebook_task:
            notebook_path: ../notebooks/01_download_sequences.py
            source: WORKSPACE

        - task_key: create_delta_tables
          max_retries: 0
          depends_on:
            - task_key: download_sequences
          job_cluster_key: cpu_cluster
          notebook_task:
            notebook_path: ../notebooks/02_create_delta_tables.py
            source: WORKSPACE

        # Serverless GPU: embeds on the single A10 the task lands on, drawing from the
        # managed serverless-GPU pool instead of the account's EC2 GPU vCPU quota
        # (which blocked the old classic 4×A10 multi_gpu_cluster). max_sequences caps
        # the corpus. Single-node is the simplest/most reliable to provision (a
        # multi-node @distributed fan-out is possible but not used here).
        - task_key: batch_embed_sequences
          max_retries: 0
          depends_on:
            - task_key: create_delta_tables
          environment_key: gpu_env
          disable_auto_optimization: true
          retry_on_timeout: false
          compute:
            hardware_accelerator: GPU_1xA10
          notebook_task:
            notebook_path: ../notebooks/03_batch_embed_sequences_sgc.py
            source: WORKSPACE

        - task_key: create_vector_index
          max_retries: 0
          depends_on:
            - task_key: batch_embed_sequences
          job_cluster_key: cpu_cluster
          notebook_task:
            notebook_path: ../notebooks/04_create_vector_index.py
            source: WORKSPACE

        # Human SwissProt companion index. gene_sequences is built by core's
        # ingest_uniprot_genes.py; we embed it (same ESM-2 model as UniRef) and
        # index it so protein search also returns human targets (e.g. PARP1).
        # ~20K rows → embed on the single A10 the task lands on (no fan-out needed).
        # Chained after batch_embed_sequences to serialise serverless-GPU use.
        - task_key: embed_gene_sequences
          max_retries: 0
          depends_on:
            - task_key: batch_embed_sequences
          environment_key: gpu_env
          disable_auto_optimization: true
          retry_on_timeout: false
          compute:
            hardware_accelerator: GPU_1xA10
          notebook_task:
            notebook_path: ../notebooks/05_batch_embed_gene_sequences_sgc.py
            source: WORKSPACE

        - task_key: create_gene_vector_index
          max_retries: 0
          depends_on:
            - task_key: embed_gene_sequences
          job_cluster_key: cpu_cluster
          notebook_task:
            notebook_path: ../notebooks/06_create_gene_vector_index.py
            source: WORKSPACE

      parameters:
        - name: catalog
          default: ${var.core_catalog_name}
        - name: schema
          default: ${var.core_schema_name}
        - name: volume_name
          default: ${resources.volumes.sequence_search_volume.name}
        - name: user_email
          default: ${var.current_user}
        - name: sql_warehouse_id
          default: ${var.sql_warehouse_id}
        # Batch-embed corpus cap (03). Production default; override per-run to cap
        # the corpus (e.g. for a quick verification run).
        - name: max_sequences
          default: "1000000"

      job_clusters:
        - job_cluster_key: cpu_cluster
          new_cluster:
            cluster_name: ""
            spark_version: 15.4.x-scala2.12
            spark_conf:
              spark.master: "local[*, 4]"
              spark.databricks.cluster.profile: singleNode
            node_type_id: ${var.cpu_node_type}
            custom_tags:
              ResourceClass: SingleNode
            enable_elastic_disk: true
            data_security_mode: SINGLE_USER
            num_workers: 0

      # Serverless GPU environment for the two embedding tasks (client "4" matches
      # the register jobs' serverless GPU AI runtime).
      environments:
        - environment_key: gpu_env
          spec:
            client: "4"

      tags: ${var.common_resource_tags}

      queue:
        enabled: true
