# Notebook (serverless GPU AI runtime) install for RFD3 registration.
# torch/CUDA + mlflow are preinstalled on the runtime — do NOT pin them here.
# rc-foundry (RFD3) is not on the private pypi-proxy; pull from public PyPI.
--extra-index-url https://pypi.org/simple
rc-foundry
biopython
