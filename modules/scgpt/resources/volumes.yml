resources:
  volumes:
    scgpt_cache_dir:
      name: ${var.cache_dir}
      catalog_name: ${var.core_catalog_name}
      schema_name: ${var.core_schema_name} # TODO: this value might need to change.
      volume_type: MANAGED