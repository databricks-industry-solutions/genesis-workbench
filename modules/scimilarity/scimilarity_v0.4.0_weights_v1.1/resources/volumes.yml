resources:
  volumes:
    scimilarity: #cache_dir for SCimilarity model
      name: ${var.cache_dir}
      catalog_name: ${var.core_catalog_name}
      schema_name: ${var.core_schema_name}
      volume_type: MANAGED